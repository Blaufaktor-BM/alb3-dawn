 {%- if cart.cart_level_discount_applications.size > 0 -%}
              <ul class="discountsb list-unstyled" role="list" aria-label="{{ 'customer.order.discount' | t }}">
                {%- for discount in cart.cart_level_discount_applications -%}  
                  <li class="discountsb__discount discountsb__discount--end">
                    Rabattcode {%- render 'icon-discount' -%}
                    {{ discountb.title | escape }} ist aktiv. Ersparnis: 
                    (-{{ discountb.total_allocated_amount | money }})
                  </li>
                {%- endfor -%}
              </ul>
            {%- endif -%}